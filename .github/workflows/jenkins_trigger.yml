name: GitHub Actions + Jenkins Monitoring

on:
  push:
    branches:
      - main
      - develop

jobs:
  monitor-jenkins:
    runs-on: ubuntu-latest
    env:
      JENKINS_URL: ${{ secrets.JENKINS_URL }}
      JENKINS_USER: ${{ secrets.JENKINS_USER }}
      JENKINS_API_TOKEN: ${{ secrets.JENKINS_API_TOKEN }}
      JENKINS_JOB_NAME: ${{ secrets.JENKINS_JOB_NAME }}
      CURRENT_BRANCH: ${{ github.ref_name }}
    steps:
      - name: Monitor Jenkins Build Status Triggered by Webhook
        run: |
          BRANCH_URL_ENCODED=$(echo "$CURRENT_BRANCH" | sed 's/\//%2F/g')
          JENKINS_JOB_PATH="$JENKINS_URL/job/$JENKINS_JOB_NAME/job/$BRANCH_URL_ENCODED"
          
          echo "🔍 Jenkins build for branch '$CURRENT_BRANCH' expected to be triggered by webhook."
          echo "Waiting for Jenkins to start build on $JENKINS_JOB_PATH..."
          
          # Aumentamos o tempo de espera inicial para garantir que o build mais recente seja registrado
          echo "Initial wait: Giving Jenkins ample time to register the new build (60 seconds)..."
          sleep 60 # Aumentado para 60 segundos
          
          JENKINS_BUILD_NUMBER=""
          MAX_RETRIES_FOR_BUILD_START=20 # Mantemos este alto (20 * 5s = 100 segundos de tentativas adicionais)
          
          LAST_BUILD_NUMBER_XPATH_URL="$JENKINS_JOB_PATH/api/xml?xpath=/*/lastBuild/number&wrapper=build"

          for i in $(seq 1 $MAX_RETRIES_FOR_BUILD_START); do
            echo "Attempt $i/$MAX_RETRIES_FOR_BUILD_START to get last build number from $LAST_BUILD_NUMBER_XPATH_URL"
          
            BUILD_NUMBER_XML_RESPONSE=$(curl -s --user "$JENKINS_USER:$JENKINS_API_TOKEN" "$LAST_BUILD_NUMBER_XPATH_URL")
          
            EXTRACTED_NUMBER=$(echo "$BUILD_NUMBER_XML_RESPONSE" | sed -n 's/.*<number>\(.*\)<\/number>.*/\1/p' | head -n 1)
          
            if [[ -z "$EXTRACTED_NUMBER" ]]; then
              EXTRACTED_NUMBER=$(echo "$BUILD_NUMBER_XML_RESPONSE" | tr -d '[:space:]')
            fi

            if [[ -n "$EXTRACTED_NUMBER" && "$EXTRACTED_NUMBER" != "null" ]]; then
              JENKINS_BUILD_NUMBER="$EXTRACTED_NUMBER"
              echo "✅ Found Jenkins build number: $JENKINS_BUILD_NUMBER"
              break
            else
              echo "⚠️ Jenkins last build number not yet available from XPath API (response: '$BUILD_NUMBER_XML_RESPONSE'). Waiting for 5 seconds..."
              sleep 5
            fi
          done

          if [[ -z "$JENKINS_BUILD_NUMBER" ]]; then
              echo "❌ Error: Could not determine Jenkins build number within expected time using XPath."
              exit 1
          fi

          echo "🔍 Monitoring Jenkins build number $JENKINS_BUILD_NUMBER at $JENKINS_JOB_PATH"
          
          BUILD_URL="$JENKINS_JOB_PATH/$JENKINS_BUILD_NUMBER/api/json"
          
          while true; do
            if ! command -v jq &> /dev/null; then
                echo "jq could not be found, please ensure it is installed in the runner environment."
                exit 1
            fi

            API_RESPONSE=$(curl -s --user "$JENKINS_USER:$JENKINS_API_TOKEN" "$BUILD_URL")
          
            if [[ -z "$API_RESPONSE" || "$(echo "$API_RESPONSE" | jq empty 2>/dev/null)" == "" ]]; then
              echo "⚠️ Jenkins API response empty or invalid JSON for build $JENKINS_BUILD_NUMBER. Retrying in 10 seconds..."
              sleep 10
              continue
            fi

            STATUS=$(echo "$API_RESPONSE" | jq -r '.result')
            BUILDING=$(echo "$API_RESPONSE" | jq -r '.building')

            if [[ "$BUILDING" == "true" ]]; then
              echo "⏳ Jenkins build still running..."
              sleep 10
            elif [[ "$STATUS" == "SUCCESS" ]]; then
              echo "✅ Jenkins Build Successful!"
              exit 0
            elif [[ "$STATUS" == "FAILURE" ]]; then
              echo "❌ Jenkins Build Failed!"
              echo "Jenkins Build Log: $JENKINS_JOB_PATH/$JENKINS_BUILD_NUMBER/console"
              exit 1
            elif [[ "$STATUS" == "UNSTABLE" ]]; then
              echo "⚠️ Jenkins Build Unstable!"
              echo "Jenkins Build Log: $JENKINS_JOB_PATH/$JENKINS_BUILD_NUMBER/console"
              exit 1
            elif [[ "$STATUS" == "ABORTED" ]]; then
              echo "⛔ Jenkins Build Aborted!"
              echo "Jenkins Build Log: $JENKINS_JOB_PATH/$JENKINS_BUILD_NUMBER/console"
              exit 1
            else
              echo "⚠️ Unexpected status or not yet finished: '$STATUS' (Building: $BUILDING). Checking again in 10 seconds..."
              sleep 10
            fi
          done